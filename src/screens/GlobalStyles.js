import { StyleSheet } from "react-native";

export const globalStyes = StyleSheet.create({
  selectBox: {
    borderWidth: 1,
    borderColor: "#ccc",
    borderRadius: 6,
    backgroundColor: "#fff",
  },
  pickerText: {
    color: "#000", // Force black text in all modes
  },
});
